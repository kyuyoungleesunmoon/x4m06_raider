# 📡 X4M06 UWB 레이더 데이터 수집 시스템

## 🎯 시스템 개요
X4M06 UWB 레이더에서 실시간 데이터를 수집, 저장, 분석하는 완전한 솔루션입니다.

## ⚡ 빠른 시작

### 1. 바로 실행 (권장)
```bash
run_x4m06.bat
```
더블클릭으로 실행 가능한 배치 파일

### 2. Python 직접 실행
```bash
conda activate x4m06_env
python launcher.py
```

## 📋 주요 구성 요소

### 🚀 실행 프로그램들

| 프로그램 | 용도 | 대상 사용자 |
|---------|------|-------------|
| `launcher.py` | 통합 메뉴 시스템 | 모든 사용자 |
| `easy_data_collector.py` | 간편 데이터 수집 | 초보자 |
| `x4m06_data_saver.py` | 고급 데이터 수집 | 고급 사용자 |
| `config_manager.py` | 설정 관리 | 시스템 관리자 |
| `hardware_data_analyzer.py` | 데이터 분석 | 연구자 |

### ⚙️ 설정 및 테스트

| 파일 | 설명 |
|------|------|
| `quick_test.py` | 빠른 연결 테스트 |
| `realtime_plot_test.py` | 실시간 플롯 테스트 |
| `x4m06_config.json` | 설정 파일 (자동 생성) |

## 🎛️ 사용 모드

### 📊 수집 모드별 설정

| 모드 | 시간 | 프레임 | 실시간 플롯 | 용도 |
|------|------|--------|-------------|------|
| 빠른 테스트 | 30초 | 100개 | ❌ | 연결 확인 |
| 표준 수집 | 2분 | 500개 | ✅ | 일반 실험 |
| 장시간 수집 | 5분 | 1000개 | ❌ | 장기 모니터링 |
| 실시간 모니터링 | 1분 | 300개 | ✅ | 라이브 관찰 |
| 사용자 정의 | 직접 설정 | 직접 설정 | 선택 | 특수 목적 |

### 🎯 탐지 범위 설정

| 범위 | DAC 설정 | 용도 |
|------|----------|------|
| 근거리 (0-2m) | 900-1000 | 정밀 근거리 탐지 |
| 중거리 (0-5m) | 900-1400 | **기본 권장 설정** |
| 장거리 (0-10m) | 900-1800 | 넓은 영역 탐지 |

## 💾 데이터 저장 구조

```
collected_data/
├── x4m06_data_[세션ID].json     # 상세 메타데이터 포함
├── x4m06_data_[세션ID].npz      # NumPy 압축 배열
└── summary_[세션ID].txt         # 텍스트 요약
```

### 📄 파일 형식 설명

- **JSON**: 완전한 메타데이터, 프레임별 상세 정보
- **NPZ**: 빠른 수치 분석용 압축 데이터
- **TXT**: 사람이 읽기 쉬운 요약 정보

## 🔧 시스템 요구사항

### 하드웨어
- ✅ X4M06 UWB 레이더 모듈
- ✅ USB 연결 케이블
- ✅ Windows 10/11 PC

### 소프트웨어
- ✅ Python 3.6 (conda 환경 권장)
- ✅ pymoduleconnector 라이브러리
- ✅ numpy, matplotlib 패키지

## 🚨 문제 해결

### 연결 문제
```
❌ COM 포트 오류
→ 장치 관리자에서 포트 번호 확인
→ 다른 프로그램에서 레이더 사용 중인지 확인

❌ 권한 오류
→ 관리자 권한으로 실행
→ 안티바이러스 예외 처리
```

### 데이터 수집 문제
```
❌ 프레임 수집 실패
→ USB 케이블 교체
→ 레이더 전원 재연결
→ Python 환경 재활성화

❌ 플롯 표시 안됨
→ matplotlib 백엔드 문제
→ 'n' 선택하여 플롯 비활성화 후 수집
```

### 성능 최적화
```
✅ 빠른 수집: 플롯 비활성화
✅ 안정적 연결: 근거리 모드 사용
✅ 배터리 절약: 낮은 FPS 설정
```

## 📊 데이터 분석 예시

### Python에서 데이터 로드
```python
import json
import numpy as np

# JSON 데이터 로드
with open('collected_data/x4m06_data_20250930_003032.json') as f:
    data = json.load(f)

# NumPy 데이터 로드
npz_data = np.load('collected_data/x4m06_data_20250930_003032.npz')
frames = npz_data['frames']

print(f"총 프레임: {len(data['frames'])}")
print(f"프레임 크기: {frames.shape}")
```

### 기본 통계 확인
```python
# 신호 범위 분석
max_values = np.max(frames, axis=1)
min_values = np.min(frames, axis=1)

print(f"최대 신호: {np.max(max_values):.3f}")
print(f"최소 신호: {np.min(min_values):.3f}")
print(f"평균 신호: {np.mean(frames):.6f}")
```

## 🎯 권장 사용 절차

### 첫 사용자
1. `run_x4m06.bat` 실행
2. "1. 간편 데이터 수집기" 선택
3. "빠른 테스트" 모드 실행
4. 수집 완료 후 데이터 분석기 실행

### 정규 사용
1. 필요에 따라 설정 관리자에서 탐지 범위 조정
2. 표준 수집 모드로 데이터 수집
3. 실시간 플롯으로 데이터 품질 확인
4. 분석기로 상세 분석 수행

### 고급 사용
1. config_manager.py에서 세밀한 파라미터 조정
2. 사용자 정의 프리셋 생성
3. 고급 데이터 수집기로 정밀 제어
4. CSV 내보내기로 외부 도구 연동

## 📞 기술 지원

### 로그 및 디버그
- 로그 파일: `logs/` 폴더
- 설정 백업: config_manager.py → 내보내기
- 오류 재현: 세션 ID와 함께 보고

### 데이터 백업
```bash
# 전체 데이터 백업
xcopy collected_data backup_YYYYMMDD /E /I

# 설정 백업
python config_manager.py → 7. 설정 내보내기
```

## 📈 성능 지표

| 항목 | 값 | 비고 |
|------|----|----- |
| 최대 FPS | 30 | 하드웨어 한계 |
| 프레임 크기 | 1488 샘플 | 고정값 |
| 신호 범위 | ±0.25 | 정규화된 값 |
| 최소 수집 시간 | 10초 | 안정성 확보 |
| 권장 수집 시간 | 60-300초 | 분석 적합성 |

---

**🎉 준비 완료! X4M06 레이더 데이터 수집을 시작하세요!**